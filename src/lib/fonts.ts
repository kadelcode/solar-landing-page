import { Inter, Roboto, Open_Sans } from 'next/font/google';

export const inter = Inter({ subsets: ['latin'] });
export const roboto = Roboto({ subsets: ['latin'], weight: '400' });
export const openSans = Open_Sans({ subsets: ['latin'], weight: '700' });
